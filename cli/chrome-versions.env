# chrome-headless-shell 版本唯一源（本文件是唯一权威，改版本只改这里）：
#   - cli/install.sh   本地克隆运行时直接 source；curl | bash 管道模式从
#                      raw.githubusercontent.com 拉取本文件后 source
#   - cli/Dockerfile   构建期 COPY 进镜像后 source（原 ARG SHELL_* 常量已废除）
#   - .github/workflows 冒烟档与正式发布同样读本文件——测试/发布/部署同一版本
# 版本口径：amd64 用 stable 152（本地端到端验证过的版本）；arm64 取 154 Beta
# （CfT 自 153.0.8001.0 才提供 linux-arm64，stable 渠道暂无 arm64）
SHELL_V_AMD64="152.0.7977.82"
SHELL_V_ARM64="154.0.8037.0"

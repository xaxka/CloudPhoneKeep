[package]
name = "cloudphonekeep"
version = "1.6.0"
description = "云手机网页版多开保活工具（移动/联通，支持多实例）"
authors = ["xixka"]
license = "MIT"
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-notification = "2"
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["net", "io-util", "time", "macros", "rt"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
opt-level = "s"
